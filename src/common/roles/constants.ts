export enum Actions {
  CREATE = 'create',
  READ = 'read',
  UPDATE = 'update',
  DELETE = 'delete',
  LIST = 'list',
  MANAGE = 'manage',
}

export enum Entities {
  ALL = 'all',
  PASSWORD = 'password',
  PROJECT = 'project',
  SCHEME = 'scheme',
  SUBJECT = 'subject',
  USER = 'user',
}
